export default function ConversationsPage() {
  return (
    <div className="flex-1 flex items-center justify-center text-[rgb(var(--color-text-secondary))] p-8">
      Select a conversation from the sidebar or create a new one
    </div>
  );
}
